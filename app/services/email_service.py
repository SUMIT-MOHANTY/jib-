def send_notification(submission) -> bool:
    # Placeholder for SMTP email notification
    # In production, configure SMTP settings and implement email sending
    print(f"[EMAIL] Notification: New contact from {submission.email}")
    return True
